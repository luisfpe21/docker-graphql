name: Docker Image CI

on:
  push:
    branches: [ "iscfer" ]
  pull_request:
    branches: [ "iscfer" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Git Semantic Version
      uses: PaulHatch/semantic-version@v5.0.1
      with:
        major_pattern: "major:"
        minor_pattern: "minor:"
        version_format: "${major}.${minor}.${patch}-prerelease${increment}"
      id: version
      
    - name: Docker Login
      env:
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
        DOCKER_PASSWD: ${{ secrets.DOCKER_PASSWD }}
        NEW_VERSION: ${{ steps.version.outputs.version }}
        NEW_VERSION2: ${{ steps.version.outputs.version_tag }}
        NEW_VERSION3: ${{ steps.version.outputs.previous_version }}
        NEW_VERSION4: ${{ steps.version.outputs.major }}
        NEW_VERSION5: ${{ steps.version.outputs.minor }}
        NEW_VERSION6: ${{ steps.version.outputs.patch }}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWD
        echo "New version: $NEW_VERSION!!!!!!!"
        echo "New version: $NEW_VERSION2!!!!!!!"
        echo "New version: $NEW_VERSION3!!!!!!!"
        echo "New version: $NEW_VERSION4!!!!!!!"
        echo "New version: $NEW_VERSION5!!!!!!!"
        echo "New version: $NEW_VERSION6!!!!!!!"
    #- name: Build the Docker image
    #  run: |
     #   docker build -t iscfer933/test-docker-graphql:0.0.2 .
      #  docker build -t iscfer933/test-docker-graphql:latest .

    #- name: Push Docker image
     # run: |
      #  docker push iscfer933/test-docker-graphql:0.0.2
       # docker push iscfer933/test-docker-graphql:latest
